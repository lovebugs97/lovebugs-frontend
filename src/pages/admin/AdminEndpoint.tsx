import { FC } from 'react';
import AdminEndpointContainer from '../../containers/admin/endpoint/AdminEndpointContainer.tsx';

const AdminEndpoint: FC = () => {
  return (
    <div>
      <AdminEndpointContainer />
    </div>
  );
};

export default AdminEndpoint;
